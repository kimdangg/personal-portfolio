<div class="well">
  <h1>Skills</h1>

  <% if @skills.any? %>
    <ul>
      <% @skills.each do |skill| %>
        <li><%= link_to skill.name, skill_path(skill) %></li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no skills.</p>
  <% end %>
</div>
